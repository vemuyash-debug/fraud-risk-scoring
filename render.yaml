services:
  - type: web
    name: fraud-risk-scoring
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python scripts/train_all.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
